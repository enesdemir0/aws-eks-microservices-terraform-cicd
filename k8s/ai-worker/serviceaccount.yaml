apiVersion: v1
kind: ServiceAccount
metadata:
  name: visionmetric-worker
  # Shared service account with k8s/gateway/serviceaccount.yaml.
  # Both gateway and ai-worker pods assume the same IRSA role for S3 access.
  # kubectl apply is idempotent — applying this file twice is harmless.
